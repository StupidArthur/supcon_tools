# single_page_batch_review — stage 7 human gate (UNSIGNED)

Status: **unsigned** — requirements only.

## Required evidence when Batch UI exists

1. Batch entry is on the **same template page** (no forced navigation to a legacy-only page).
2. **progress**, failure reason, result summary, and export path are discoverable.
3. Failure must not render as an **empty success** chart.
4. Realtime vs Batch **mutex** state is visible and understandable.
5. Export uses a **CSV** dialog (not YAML file type).
6. Large result charts remain operable (downsample applied).

## Notes

Prospective until stage 7 business UI is complete. Do not finalize/sign in this batch.
